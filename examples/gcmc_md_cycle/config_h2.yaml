# Pure H2 on Fe — GCMC half of the cycle (template; @TOKENS@ filled by run_cycles.sh)
energy_backend: mace
bdir: "@BDIR@"
model_dir: "@MODEL@"

restart_path: "@RESTART@"

elements: [Fe, H]
gas_list: [H2]
gas_template_dir: "@EXDIR@/templates/"

z_cutoff: 15.14698231002696
temperature: 723.0

pressure: 150.0
mole_fractions:
  H2: 1.0
pressure_unit: atm

steps: 10
seed: 42
log_mu_diagnostics: true
